import { useState } from 'react';
import { ProjectCard } from '../components/ProjectCard';
import { projects } from '../data/siteData';
export function ProjectsPage(){const [filter,setFilter]=useState<'all'|'active'|'finished'>('all');const items=projects.filter(p=>filter==='all'||(filter==='active'&&p.active)||(filter==='finished'&&p.status==='Zakończony'));return <div className="page-wrap inner-page"><div className="page-hero"><span className="section-kicker">PROJEKTY</span><h1>Od pomysłu do działającej rzeczy.</h1><p>Tu znajdziesz wszystkie projekty — aktywne, planowane i te, które kiedyś trafią do archiwum.</p></div><div className="toolbar"><div className="segmented"><button className={filter==='all'?'active':''} onClick={()=>setFilter('all')}>Wszystkie</button><button className={filter==='active'?'active':''} onClick={()=>setFilter('active')}>Aktywne</button><button className={filter==='finished'?'active':''} onClick={()=>setFilter('finished')}>Zakończone</button></div><span>{items.length} projektów</span></div><div className="projects-grid">{items.map(p=><ProjectCard key={p.slug} project={p}/>)}</div></div>}

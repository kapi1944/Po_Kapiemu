import { Link } from 'react-router-dom';
import type { Project } from '../data/siteData';
import { Icon } from './Icons';
export function ProjectCard({project}:{project:Project}){return <article className={`project-card accent-${project.accent} ${project.locked?'locked':''}`}><div className="project-visual"><div className="visual-grid"/><div className="visual-orb one"/><div className="visual-orb two"/><div className="visual-symbol">{project.locked?<Icon name="lock" size={34}/>:project.title.slice(0,2).toUpperCase()}</div><span className="status-chip">{project.status}</span></div><div className="project-card-body"><span className="eyebrow">{project.eyebrow}</span><h3>{project.title}</h3><p>{project.description}</p><div className="project-progress-label"><span>Postęp</span><strong>{project.progress}%</strong></div><div className="progress-track"><span style={{width:`${project.progress}%`}}/></div><div className="project-card-footer">{project.locked?<button className="text-link muted" disabled><Icon name="lock" size={16}/> Zaloguj się, aby zobaczyć</button>:<Link className="text-link" to={`/projekty/${project.slug}`}>Otwórz projekt <Icon name="arrow" size={16}/></Link>}</div></div></article>}
